# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## Project-manager review before coding

Read `docs/mobile/13-PM-REVIEW-PR60-FOR-ASTRA.md` at the start of every session. It lists the open problems the PM found in PR #60 (blocking items first), the owner decisions you must not change in code, and the working rules (small PRs against `main`, plan gates stand).
