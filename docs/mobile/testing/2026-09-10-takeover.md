# Mobile Takeover Verification - 2026-09-10

Updated 2026-09-11 after native simulator smoke. This ledger records both
successful checks and acceptance gates that remain open.

## Executed Locally

| Check | Result |
|---|---|
| Root TypeScript | Passed after excluding the independent native package |
| Root Vitest | 1,467 tests across 81 files passed |
| Mobile TypeScript / lint | Passed |
| Mobile Vitest | 71 tests across 8 files passed |
| Expo dependency compatibility | `expo install --check` passed |
| Expo Doctor | 21/21 passed |
| iOS production JS export | Passed, 1,762 modules |
| Release credential inspection | 74 exported files checked; neither of the two configured development credential values found |
| Native generation | SDK 57 iOS prebuild and CocoaPods install passed |
| Native Xcode Debug build | Passed, installed on iPhone 17 Pro / iOS 26.5 simulator; local Sentry upload disabled |
| Native UI | Login, AI consent/disclaimer, Home, restored question, draft recovery, incomplete results, Progress, Settings, and expired-trial modals verified |
| GitHub CI | Mobile and root jobs passed at `05b85a1`; draft PRs #60 and #59, no main merge |
| EAS account | CLI reports Not logged in |

Production export is not a signed IPA, and mocked native adapter tests do not
prove physical audio behavior. Xcode dependency warnings must be distinguished
from application build errors.

## Simulator Evidence (September 10-11)

Maestro 2.10.0 is installed locally under `/private/tmp/heydpe-maestro/maestro`.
The Homebrew install stopped at its Command Line Tools check. The same downloaded
release ZIP was SHA-256-verified and extracted, using the existing Java runtime;
no system toolchain replacement was performed.

- Authenticated the dedicated development account using the existing Debug-only
  route. No real-user OTP email was sent. This is not native Apple/OAuth evidence.
- Accepted the separate AI and disclaimer gates. Reauthentication reached Home
  with those consents persisted server-side.
- Restored the account's June exam and its pending question. A text submission
  was blocked by `session_expired`, showing **Trial window closed**. The exact
  typed draft remained in the input after dismissing the modal.
- Ended that empty QA session through the UI. Results showed **Incomplete
  coverage**, zero scores, and Progress refreshed to show one completed session.
- Opened Settings on live account data with analytics still off. No account
  deletion, subscription change, or paid-equivalent override was exercised.
- Starting another exam showed **Your free trial ended**, then returned to Home.
- A host network interruption produced the offline banner. A clean app restart
  and QA sign-in restored connectivity. Controlled in-session reconnect remains
  unverified; this was not a physical-device lifecycle test.

The final navigation/trial flow passed in
`/private/tmp/heydpe-maestro-resume-output/2026-09-11_010548`.
The committed `.maestro/trial-expired.yaml` also passed in a separate run;
its logs and screenshots are under `/private/tmp/heydpe-maestro-trial-output`.
Screenshots are preserved locally, gitignored, in
`apps/mobile/.screenshots/takeover-2026-09-10/`. Login legal-link spacing was
corrected and visually rechecked. The floating Expo developer toolbar initially
intercepted onboarding controls; the simulator was launched with the development
preference `-EXDevMenuShowFloatingActionButton NO`. Input/role labels also differed
from plain visible text; stable IDs now cover exam input, send, busy, and end.

**Not a successful graded text-exam smoke:** the ordinary QA account is expired.
The committed `exam-smoke.yaml` still needs a release simulator build and a
dedicated authenticated account with current exam access. It deliberately fails
when no assessment is received rather than treating a paywall as success.

## Not Yet Verified

- Signed development client on a registered physical iPhone.
- Actual TestFlight build and installation on two iPhones.
- Native Apple sign-in, repeat sign-in, real Keychain cold-launch persistence.
- Five-exchange physical voice exam, T1/T2/T3/T4 and lifecycle matrix.
- Long-utterance prefetch/sentence-streaming decision, conditional on measurements.
- Sentry source-map upload and crash ingestion; production analytics events.
- Physical-device trial/quota/offline/409/503 matrix. Only the simulator's
  expired-session and expired-trial paths above have UI evidence.
- Maestro release smoke. The prepared flow assumes prior QA authentication;
  no fixed-OTP fixture or current-access QA account was supplied.
- Full VoiceOver/Dynamic Type pass, payments, store review, Android.

## Scope And Safety

- No main deployment, account deletion, purchase, Apple key rotation, or tester
  entitlement grant was performed.
- Original handoff and ops files are preserved. No secrets are committed or
  copied into this evidence file.
- Source work beyond the Phase 3 dependency is preparatory, not acceptance.
- Compatible transitive security patches reduced npm audit from 20 findings
  (4 high) to 15 moderate, zero high/critical. Remaining advisory chains include
  the build-time xcode/uuid dependency and expo-router/query-string decoding.
  They remain a release-review item; no force downgrade or SDK-major change was used.
