# Mobile Takeover Verification - 2026-09-10

## Executed Locally

| Check | Result |
|---|---|
| Root TypeScript | Passed after excluding the independent native package |
| Root Vitest | 1,467 tests across 81 files passed |
| Mobile TypeScript / lint | Passed |
| Mobile Vitest | 71 tests across 8 files passed |
| Expo dependency compatibility | `expo install --check` passed |
| Expo Doctor | 21/21 passed |
| iOS production JS export | Passed, 1,762 modules |
| Release credential inspection | 74 exported files checked; neither of the two configured development credential values found |
| Native generation | SDK 57 iOS prebuild and CocoaPods install passed |
| Native Xcode Debug build | Passed, installed on iPhone 17 Pro / iOS 26.5 simulator; local Sentry upload disabled |
| EAS account | CLI reports Not logged in |

Production export is not a signed IPA, and mocked native adapter tests do not
prove physical audio behavior. Xcode dependency warnings must be distinguished
from application build errors.

## Not Yet Verified

- Signed development client on a registered physical iPhone.
- Actual TestFlight build and installation on two iPhones.
- Native Apple sign-in, repeat sign-in, real Keychain cold-launch persistence.
- Five-exchange physical voice exam, T1/T2/T3/T4 and lifecycle matrix.
- Long-utterance prefetch/sentence-streaming decision, conditional on measurements.
- Sentry source-map upload and crash ingestion; production analytics events.
- Ordinary-account trial/quota/offline/409/503 screenshots on devices.
- Maestro release smoke. The prepared flow assumes prior QA authentication;
  no fixed-OTP fixture or Maestro installation has been verified.
- Full VoiceOver/Dynamic Type pass, payments, store review, Android.

## Scope And Safety

- No main deployment, account deletion, purchase, Apple key rotation, or tester
  entitlement grant was performed.
- Original handoff and ops files are preserved. No secrets are committed or
  copied into this evidence file.
- Source work beyond the Phase 3 dependency is preparatory, not acceptance.
- Compatible transitive security patches reduced npm audit from 20 findings
  (4 high) to 15 moderate, zero high/critical. Remaining advisory chains include
  the build-time xcode/uuid dependency and expo-router/query-string decoding.
  They remain a release-review item; no force downgrade or SDK-major change was used.
