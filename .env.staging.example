# =============================================================================
# HeyDPE — Staging Environment Variables
# =============================================================================
# Copy to .env.local when working against your STAGING Supabase project.
#
# IMPORTANT: These must point to a SEPARATE Supabase project, not production.
# After setting up, run:
#   INSERT INTO system_config (key, value)
#   VALUES ('app.environment', '{"name":"staging"}');
# in the staging database to enable environment verification.

# --- Environment identification (required for staging) ---
NEXT_PUBLIC_APP_ENV=staging

# --- Supabase (STAGING project — different from production!) ---
NEXT_PUBLIC_SUPABASE_URL=https://your-staging-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-staging-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-staging-service-role-key

# --- Anthropic (same key is fine for staging) ---
ANTHROPIC_API_KEY=sk-ant-...

# --- OpenAI (same key is fine for staging) ---
OPENAI_API_KEY=sk-...

# --- PostHog (use a separate staging project or omit) ---
# POSTHOG_PERSONAL_API_KEY=phx_...

# --- Resend (use test mode or omit for staging) ---
# RESEND_API_KEY=re_...
# RESEND_INBOUND_API_KEY=re_...
# RESEND_WEBHOOK_SECRET=whsec_...
# EMAIL_FORWARD_TO=your-personal@email.com

# --- Stripe (use TEST keys for staging, never live keys) ---
# STRIPE_SECRET_KEY=sk_test_...
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
